# Fleet canary roster: every known Java repo consuming sava-build's hardening
# plugin. `tools/fleet-canary.sh` and `tools/local-fuzz.sh` use the locally
# available sibling checkouts in ordinary mode; `--release` requires every
# entry and a clean checkout at the declared GitHub remote. It writes an
# immutable SHA-bound run bundle under build/hardening and atomically points the
# canonical receipt at that bundle.
# Pass `--deep` when an explicit repeated diagnostic is worth its cost; strict
# release certification itself runs every suite once and does not require a soak.
#
# Format: one repo per line, `<github-org>/<repo>`, optional
# ` deep=<pitestSuiteTask>`. '#' lines and blank lines are ignored.
sava-software/sava
sava-software/ravina
sava-software/json-iterator deep=pitestUtil
sava-software/http-servers
sava-software/idl-src-gen
sava-software/incident-client
sava-software/idl-clients
glamsystems/ix-proxy
glamsystems/glam-sdk-java
glamsystems/vault-stat-service
