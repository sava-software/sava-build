# Fleet canary roster: the consumer repos validated against an unreleased
# sava-build before a release PR merges. This file exists to answer "which
# repos does a canary run cover" without a hand-kept list in anyone's head —
# `tools/fleet-canary.sh` with NO arguments reads it and resolves each repo
# as a sibling checkout of this one (../<repo>); pass `--deep` to also honour
# the ` deep=<pitestSuiteTask>` annotations (two real mutation runs cycling
# the machine-local status stash — see the script's usage notes).
#
# Format: one repo per line, `<github-org>/<repo>`, optional
# ` deep=<pitestSuiteTask>`. '#' lines and blank lines are ignored.
sava-software/sava
sava-software/ravina
sava-software/json-iterator deep=pitestUtil
sava-software/http-servers
sava-software/idl-src-gen
sava-software/incident-client
glamsystems/ix-proxy
glamsystems/vault-stat-service
