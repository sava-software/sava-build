# Local fuzz roster: every known Java repo consuming sava-build's hardening plugin.
# `tools/local-fuzz.sh` uses locally available sibling checkouts in ordinary mode;
# its explicit `--release` experiment requires every entry and a clean checkout at
# the declared GitHub remote, then writes an immutable SHA-bound run bundle.
#
# Format: one repo per line, `<github-org>/<repo>`. '#' lines and blank lines are ignored.
sava-software/sava
sava-software/ravina
sava-software/json-iterator
sava-software/http-servers
sava-software/idl-src-gen
sava-software/incident-client
sava-software/idl-clients
glamsystems/ix-proxy
glamsystems/glam-sdk-java
glamsystems/vault-stat-service
